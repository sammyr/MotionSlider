#pragma once

#include <QtGlobal>
#include <QString>

class ScrollConfig {
public:
    // Prozentualer Anteil (0-100) der unteren Bereichshöhe für Navigation
    static int navigationAreaPercent();
};
